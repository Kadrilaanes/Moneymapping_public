EB_APP_ID=
EB_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
APP_URL=https://moneymap.example
DATABASE_URL=postgres://user:pass@host/db

# Long random string. Gates the whole app.
AUTH_PASSWORD=
# Separate secret so the cron can call /api/sync without the login cookie.
CRON_SECRET=
